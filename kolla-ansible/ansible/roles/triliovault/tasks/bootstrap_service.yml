---

- name: Running triliovault datamover api bootstrap container
  vars:
    triliovault_datamover_api: "{{ triliovault_services['triliovault-datamover-api'] }}"
  become: true
  kolla_docker:
    action: "start_container"
    common_options: "{{ docker_common_options }}"
    detach: False
    environment:
      KOLLA_BOOTSTRAP:
      KOLLA_CONFIG_STRATEGY: "{{ config_strategy }}"
    image: "{{ triliovault_datamover_api.image }}"
    labels:
      BOOTSTRAP:
    name: "bootstrap_triliovault_datamover_api"
    restart_policy: "no"
    volumes: "{{ triliovault_datamover_api.volumes|reject('equalto', '')|list }}"
  run_once: True
  delegate_to: "{{ groups[triliovault_datamover_api.group][0] }}"


- name: Running triliovault workloadmgr api bootstrap container
  vars:
    triliovault_wlm_api: "{{ triliovault_services['triliovault-wlm-api'] }}"
  become: true
  kolla_docker:
    action: "start_container"
    common_options: "{{ docker_common_options }}"
    detach: False
    environment:
      KOLLA_BOOTSTRAP:
      KOLLA_CONFIG_STRATEGY: "{{ config_strategy }}"
    image: "{{ triliovault_wlm_api.image }}"
    labels:
      BOOTSTRAP:
    name: "bootstrap_triliovault_wlm_api"
    restart_policy: "no"
    volumes: "{{ triliovault_wlm_api.volumes|reject('equalto', '')|list }}"
  run_once: True
  delegate_to: "{{ groups[triliovault_wlm_api.group][0] }}"
  register: result_wlm_api

- name: Running triliovault workloadmgr workloads bootstrap container
  vars:
    triliovault_wlm_workloads: "{{ triliovault_services['triliovault-wlm-workloads'] }}"
  become: true
  kolla_docker:
    action: "start_container"
    common_options: "{{ docker_common_options }}"
    detach: False
    environment:
      KOLLA_BOOTSTRAP:
      KOLLA_CONFIG_STRATEGY: "{{ config_strategy }}"
    image: "{{ triliovault_wlm_workloads.image }}"
    labels:
      BOOTSTRAP:
    name: "bootstrap_triliovault_wlm_workloads"
    restart_policy: "no"
    volumes: "{{ triliovault_wlm_workloads.volumes|reject('equalto', '')|list }}"
  run_once: True
  delegate_to: "{{ groups[triliovault_wlm_workloads.group][0] }}"
  register: result_wlm_workloads


- name: Running triliovault workloadmgr scheduler bootstrap container
  vars:
    triliovault_wlm_scheduler: "{{ triliovault_services['triliovault-wlm-scheduler'] }}"
  become: true
  kolla_docker:
    action: "start_container"
    common_options: "{{ docker_common_options }}"
    detach: False
    environment:
      KOLLA_BOOTSTRAP:
      KOLLA_CONFIG_STRATEGY: "{{ config_strategy }}"
    image: "{{ triliovault_wlm_scheduler.image }}"
    labels:
      BOOTSTRAP:
    name: "bootstrap_triliovault_wlm_scheduler"
    restart_policy: "no"
    volumes: "{{ triliovault_wlm_scheduler.volumes|reject('equalto', '')|list }}"
  run_once: True
  delegate_to: "{{ groups[triliovault_wlm_scheduler.group][0] }}"
  register: result_wlm_scheduler


- name: Running triliovault workloadmgr cron bootstrap container
  vars:
    triliovault_wlm_cron: "{{ triliovault_services['triliovault-wlm-cron'] }}"
  become: true
  kolla_docker:
    action: "start_container"
    common_options: "{{ docker_common_options }}"
    detach: False
    environment:
      KOLLA_BOOTSTRAP:
      KOLLA_CONFIG_STRATEGY: "{{ config_strategy }}"
    image: "{{ triliovault_wlm_cron.image }}"
    labels:
      BOOTSTRAP:
    name: "bootstrap_triliovault_wlm_cron"
    restart_policy: "no"
    volumes: "{{ triliovault_wlm_cron.volumes|reject('equalto', '')|list }}"
  run_once: True
  delegate_to: "{{ groups[triliovault_wlm_cron.group][0] }}"
  register: result_wlm_cron


##Create_Cloud_Admin_Trust

- name: Create Cloud Admin Trust
  shell: |
    . /opt/triliovault-cloudrc && workloadmgr trust-create --is_cloud_trust True admin --insecure
  register: crt_cld_trust
  run_once: True
  delegate_to: "{{ groups[triliovault_wlm_api.group][0] }}"
  when: (result_wlm_api.rc == 0) and (result_wlm_workloads.rc == 0) and (result_wlm_scheduler.rc == 0) and (result_wlm_cron.rc == 0)

- name: Print Cloud admin trust output
  debug:
    msg: "{{ crt_cld_trust }}"
