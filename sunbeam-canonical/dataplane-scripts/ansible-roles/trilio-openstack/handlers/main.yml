---
- name: restart tvault-contego
  ansible.builtin.systemd:
    name: "{{ trilio_datamover_service }}"
    state: restarted
    daemon_reload: true
  become: true

- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  become: true
