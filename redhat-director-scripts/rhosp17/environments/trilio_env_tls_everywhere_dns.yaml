# *******************************************************************
# This file was created automatically by the sample environment
# generator. Developers should use `tox -e genconfig` to update it.
# Users are recommended to make changes to a copy of the file instead
# of the original, if any customizations are needed.
# *******************************************************************
# title: Deploy All SSL Endpoints as DNS Names
# description: |
#   Use this environment when deploying an overcloud where all the endpoints are
#   DNS names and there's TLS in all endpoint types.
parameter_merge_strategies:
  EndpointMap: merge

parameter_defaults:
  # Mapping of service endpoint -> protocol. Typically set via parameter_defaults in the resource registry.
  # Type: json
  EndpointMap:
    TriliovaultDatamoverAdmin: {protocol: 'https', port: '8784', host: 'CLOUDNAME'}
    TriliovaultDatamoverInternal: {protocol: 'https', port: '8784', host: 'CLOUDNAME'}
    TriliovaultDatamoverPublic: {protocol: 'https', port: '13784', host: 'CLOUDNAME'}
    TriliovaultWlmAdmin: {protocol: 'https', port: '8781', host: 'CLOUDNAME'}
    TriliovaultWlmInternal: {protocol: 'https', port: '8781', host: 'CLOUDNAME'}
    TriliovaultWlmPublic: {protocol: 'https', port: '13781', host: 'CLOUDNAME'}
