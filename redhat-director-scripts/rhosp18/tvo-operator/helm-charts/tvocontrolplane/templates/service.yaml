apiVersion: v1
kind: Service
metadata:
  name: {{ include "tvocontrolplane.fullname" . }}
  labels:
    {{- include "tvocontrolplane.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tvocontrolplane.selectorLabels" . | nindent 4 }}
