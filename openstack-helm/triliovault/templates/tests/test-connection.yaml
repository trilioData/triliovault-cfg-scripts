apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "triliovault.fullname" . }}-test-connection"
  labels:
{{ include "triliovault.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "triliovault.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
